# PROJECT REGISTRY — Tool 1: Multilingual Planning & Pre-Generation System

> **This file is the cross-conversation source of truth.** Every Claude session must read this first and update it before ending.

---

## Core Idea

Tool 1 is the **multilingual planning and pre-generation engine** of the Creator Studio project. It takes a single script and produces everything needed to generate videos in multiple languages:

- Translated scripts (per language)
- TTS narrations (per language)
- Subtitle alignment & SRT files (per language)
- Master scene planning (shared across languages)
- Localized timelines (per language, same scenes, different durations)
- Prompt lists for image/video asset generation (shared)

**The fundamental principle:** Scenes are defined ONCE by the master language. All other languages reuse the same scene structure but with timing adjusted to their narration duration.

**Consistency guide is per-episode** (not shared at niche project level).

## Two-Tool Architecture

- **Tool 1** (this project) — Planning & pre-generation: translation → TTS → alignment → scene planning → timeline → prompts
- **Tool 2** (separate) — Final video assembly: takes Tool 1 outputs + shared assets → produces final localized videos
- **Integration status (2026-04-05)** — `IMPLEMENTATION_PLAN_TOOL2_INTEGRATION.md` is active. Backend Phases 0-5 plus the Codex-owned Phase 10 safety tasks are complete: Tool 2 core render modules were ported into `tool1_dashboard/video_assembly/`, Tool 1 startup health now reports FFmpeg availability, the SQLite layer now has dedicated Tool 2 tables for render jobs/shared scene assets/render logs, shared asset upload + validation APIs are live, sequential single-language or `all` renders run inside Tool 1, successful renders auto-clean `temp/scenes/`, and the assembly backend now exposes explicit render-job deletion plus per-episode temp cleanup endpoints.

## Current State (as of 2026-04-08)

### What Exists & Works
- **Dashboard app** (`tool1_dashboard/`) — FastAPI-based, Kanban-style pipeline UI
  - Unified backend with service layer + SQLite
  - Dark/light theme, responsive layout
  - Primary workflow is now project-scoped: `Niche Projects -> Project board -> Draft episode -> Episode overlay -> explicit workflow start`
  - Context cleanup on 2026-04-04: outdated root specs that described a Google Stitch design brief and a future Tool 2 merge were archived out of the project root so future AI work stays anchored on the current README/registry instead of superseded concepts
  - Views: Niche Projects, project board/detail, episode overlay/direct episode route, Voice Profiles, Translation Profiles, Settings, Templates
  - Legacy Jobs and Projects/Builds models have been fully removed; the old global Pipeline Board is no longer a primary workflow surface.
  - Drawbridge feedback repair on 2026-03-26: the project-board CTA now says `Create episode`, the Draft column add action is a compact `+` with hover copy, column helper text moved into title hover tooltips, and the create-project modal now uses a searchable target-language picker instead of a checkbox wall
  - Translation Profiles rework on 2026-03-26/27: the setup modal now has dedicated provider-mode cards, OpenAI model discovery, searchable/sortable model selection, masked saved-key edit flow, preview-only `Codex CLI` / `Claude Code CLI` tabs, and compact summary cards that only show name/provider/model/readiness while opening the existing modal for details on click
  - Shell controls polish on 2026-03-27: the global refresh/theme quick actions moved out of the top-right chrome and into the sidebar as compact utility buttons, keeping the page header focused on the current workspace title
  - Codex local-environment setup on 2026-03-28: the repo now ships `.codex/environments/environment.toml` with a shared header `Run Tool 1 Dashboard` action that executes `python run_tool1_dashboard.py`, so the Codex app can surface the project Run button without manual per-conversation setup
  - Episode workflow-launch cleanup on 2026-03-27: queue wording is now explicit workflow wording, board/overlay start controls are compact icon-only actions with hover explanations, readiness panels are state-aware, and overlay feedback now reconciles fast backend failures instead of leaving stale success copy behind
  - Project-kanban workflow feedback pass on 2026-03-27: episode cards now show an always-visible inline workflow status derived from real `pipeline_status`, `current_stage`, `updated_at`, and `language_statuses`; started cards leave `Draft` immediately by using the same computed display stage for status and column placement; active boards poll at 1 second while any episode is `queued`, `running`, or `paused_for_tts`
  - Drawbridge paused-TTS column fix on 2026-03-27: cards paused on narration now respect `current_stage = "tts"` on the Kanban instead of snapping back to the original queued stage, so paused work renders under `TTS / Voice` with live per-language TTS progress
  - TTS visibility follow-up on 2026-03-28: episode payloads now expose structured active-job telemetry (`language_code`, `current_chunk`, `total_chunks`, `percent`, `worker_active`), and both the Kanban card and episode detail/overlay render a live narration meter so long XTTS runs no longer look idle while the worker is actively generating chunks
  - Workflow control pass on 2026-03-28: episode detail/overlay now include a dedicated control panel with `Start/Resume workflow`, `Run selected step`, and `Pause after current step`; backend restarts now default to the actual stopped stage instead of blindly reusing `consistency_guide`, explicit selected-step reruns rewind downstream outputs, and manual pause requests land only at safe stage boundaries (including after async TTS completes)
  - Drawbridge output-files panel fix on 2026-03-28: the episode detail/overlay files section now auto-syncs from live frontend state instead of relying on DOM-only refresh mutations, the manual refresh button is gone, nested files are listed without collapsing duplicate names, empty files are labeled clearly, and text/JSON/audio outputs now open inline with a direct download action
  - Drawbridge output-files scroll fix on 2026-03-28: the episode output-files listbox now preserves its own scroll position across full rerenders and local auto-sync refreshes, and wheel chaining is contained inside the list so long artifact lists no longer jump back to the top while the overlay refreshes
  - Drawbridge overlay/TTS follow-up on 2026-03-28: the episode overlay is now a single-column surface with inline `Stage runs`, duplicated sidebar quick actions are gone, multi-language TTS queue entry now submits all eligible languages before `paused_for_tts`, and paused-TTS reconciliation backfills legacy missing jobs or fails unrecoverable states instead of leaving cards falsely stuck in `running`
  - Drawbridge paused-TTS stale recovery on 2026-03-28: paused episodes now requeue stale `processing` narration jobs after restart, wake the shared TTS worker back up for queued multilingual narration, and fail visibly if queued narration cannot resume instead of sitting forever on a frozen chunk counter like `ES 17/328`
  - Drawbridge overlay live-run feedback fix on 2026-03-27: episode overlays now surface a visible `Live activity` panel plus an auto-opened stage-run snapshot box, so running provider stages show provider/model/workdir/artifact metadata even before stdout/stderr previews exist
  - Drawbridge live-activity signal clarity pass on 2026-03-28: stage-run payloads now expose stdout/stderr preview file timestamps and sizes, and the episode detail surfaces split `Run age`, `Output source`, `Last preview write`, and dedicated preview cards so users can tell the difference between real artifact output and a fallback execution snapshot
  - Provider CLI stall guard on 2026-03-27: structured `codex` / `claude` runs now enforce a configurable subprocess timeout, and the background worker auto-fails stale provider stage-runs into `Needs Attention` so episodes do not sit in `Consistency Guide` forever when the CLI hangs or never emits output
  - Workflow-stage provider expansion on 2026-03-27: scene planning, consistency guide, video prompts, and image prompts now accept `OpenAI API` as a first-class provider with a shared saved API key in Settings, refreshable OpenAI model discovery, editable model-id fields so newer CLI models can be typed manually, and OpenAI Responses API execution for structured stage runs
  - Scene-planning malformed-tail recovery on 2026-04-03: chunk validation now trims an obviously broken trailing suffix from LLM scene output instead of failing the whole stage when the remainder is only zero/negative-duration spillover; live episode `205` was reprocessed through `scene_planning` on the patched code and is now paused cleanly at `video_prompt_generation` with a fresh `timeline_draft.json`
  - Italian alignment recovery on 2026-04-03: the MFA config now uses the real Italian model ids (`italian_cv`), per-language retry now supports `alignment` and `timeline_mapping`, skipped timelines preserve the upstream alignment error instead of masking it, and live episodes `204` + `205` were repaired by rerunning only Italian through SRT + timeline generation
  - Desktop shell/runtime ownership on 2026-04-03: `run_tool1_dashboard.py` now defaults to a single-instance desktop launcher (`pywebview`) instead of a browser-tab-only flow, the Settings page exposes `App shell`, `App process`, and `Voice engine` runtime cards via `/api/app-runtime`, and the runtime metadata now explicitly tells the user whether closing the window will stop the backend
- **TTS module** (`tool1_dashboard/tts/`) — audio, chunker, constants, manager, worker (XTTS-v2)
  - **Runtime Fixed (2026-03-26)**: Resolved compatibility issues with `torch 2.6.0` and `transformers 5.x`. Environment now pinned to `torch 2.3.1` and `transformers 4.39.3`. Missing dependencies (`bangla`, `gruut`, `spacy[ja]`, `umap-learn`) manually restored.
- **Translation module** (`tool1_dashboard/translation/`) — adapter, chunker, prompts, service
  - GPT-5/OpenAI translation safeguard on 2026-04-02: `Responses API` translation calls now use minimal reasoning + low verbosity for GPT-5 models, blank or `incomplete` responses fail fast instead of being accepted as success, and the pipeline no longer persists zero-byte translated scripts that later fail in `TTS / Voice`
  - Episode `205` repair pass on 2026-04-04: scene-aware translation retries now preserve the full source script when `master_scenes` omit connective paragraphs, translation audits reject mixed-language CTA leakage while keeping configured per-language channel names, and `gpt-5.4*` translation calls now use `reasoning = low` because that model family does not accept `minimal`
  - Multilingual quality upgrade on 2026-04-04: translation now uses shared per-language rulepacks, deterministic chunk/script audits, a fixed OpenAI reviewer pass (`gpt-5.4-mini`), fail-closed repair behavior, readable + spoken script artifacts, spoken reference/number normalization for TTS/alignment, and export packaging that includes both readable and spoken scripts
  - OpenAI long-chunk retry on 2026-04-04: when a Responses API translation call ends with `status = incomplete` because of `max_output_tokens`, the adapter now retries once with a larger output budget before failing the language
  - Reviewer-sanity follow-up on 2026-04-04: script-review prompts now require exact translated evidence, structured reviewer issues are normalized instead of blindly stringified, unsupported source-channel complaints are pruned, and contradictory reviewer failures with all scores `>= 4` now downgrade to pass-with-suggestions instead of blocking a good script
- **Alignment tool** (`tool1_dashboard/alignment_tool/`)
  - Subtitle-density hardening on 2026-04-04: subtitle segmentation now uses a deterministic DP readability-first segmenter, shared language-rulepack break rules, timing-gap redistribution for dense cues, and richer `alignment_report.json` density diagnostics (`segment_profile`, `segments_over_*_cps`, fast-segment buckets, averages, optimization passes)
  - French-first subtitle cleanup follow-up on 2026-04-05: added a fast segmentation-only benchmark path that reuses existing `words.json` + `segments.json`, lets the segmenter seed from an existing cue layout, repairs only the local 3-block neighborhood around the worst dense cues, and re-optimizes silence-gap distribution pairwise between adjacent subtitle blocks
- **SRT chunker** (`tool1_dashboard/srt_chunker/`)
- **Episode pipeline** — all 10 stages implemented in `_process_episode()` (service.py)
- **Queue readiness guardrails**
  - Queue/requeue is blocked when the project is not runnable
  - Episodes and project detail payloads now include structured `queue_readiness`
  - Queue blockers are surfaced on project cards and inside the episode overlay
- **TTS runtime guardrails**
  - Voice profile creation now skips latent precompute when XTTS runtime is unavailable instead of queuing dead jobs
  - Voice-test submission now fails fast with an actionable runtime error instead of leaving jobs permanently queued
  - Voice-engine health now surfaces missing XTTS dependencies and failed auto-start attempts directly in the UI
  - Voice-engine health now also exposes runtime device/build metadata plus active/queued `generate` counts, and paused-TTS views render that telemetry so users can see CPU-vs-GPU and queue depth directly
  - Voice profiles are now language-agnostic, create with only name + reference audio, and expose a one-click `Play test` action that generates a fresh inline sample without manual text entry
  - Voice-profile auto-refresh now pauses while a preview clip is actively playing, so inline playback is not interrupted by the 5-second dashboard refresh loop
  - Voice/TTS worker lifecycle is now automatic: interactive voice-profile actions wake the engine on demand and let it sleep shortly after profile/test work finishes, while pipeline `generate` work keeps the engine warm until the queued TTS batch drains
  - TTS pacing is now configured per voice profile with saved presets plus advanced controls, and both `Play test` and production narration use the same resolved config path
  - The repo TTS chunker is now the authoritative narration split layer; queued `generate` and `test_voice` jobs carry explicit chunk text plus a frozen `tts_config` snapshot, and XTTS internal `enable_text_splitting` is disabled
  - TTS chunk sizing now honors the saved voice-profile tuning for both `Play test` and production `generate` jobs, and manual tuning is clamped to an XTTS-safe maximum of `250` chars so production no longer inflates profiles into truncation-risk territory
  - The TTS manager now enforces a single live worker globally: fresh duplicate worker heartbeats are detected across the whole DB, extra worker PIDs are terminated, and their active jobs are requeued immediately so multilingual narration stays one-language-at-a-time instead of silently running in parallel
  - Paused-TTS recovery now requeues orphaned `processing` jobs whose worker has no fresh heartbeat even when another worker is healthy, and duplicate worker starts are blocked when a fresh shared-worker heartbeat already exists
  - The dashboard Python environment was repaired on 2026-03-29 to use `torch 2.3.1+cu121` / `torchaudio 2.3.1+cu121`; runtime verification now reports `device = cuda` on the RTX 3050 Laptop GPU
  - The voice-profile card action row is now fully compact: play, tuning, and delete use icon-only controls with hover tooltips, and active play-test states stay visually tight instead of expanding into large text-button blocks
  - Operational visibility note on 2026-04-04: long-form XTTS runs execute in a detached background worker (`python -m tool1_dashboard.tts.worker`), so the shell can sit at a normal prompt while narration is still active; the authoritative live signals are `worker_heartbeats`, `tts_jobs.progress`, and fresh files under `workspace/tts/chunks/<job_id>/`, while `workspace/tts/worker.log` can look stale because stdout is buffered
- **Stage-run logging for provider stages**
  - consistency guide, scene planning, and prompt generation now preserve structured stage runs and full failure details
- **Template/settings reads**
  - template and settings reads are now side-effect free; reads no longer upsert template rows
- **Interaction responsiveness**
  - route changes and episode overlay opens now render immediately with explicit loading states instead of waiting for the full refresh cycle
  - dashboard refreshes now reuse short-lived frontend cache windows for health/settings metadata and skip legacy board fetches unless the board view is active
  - smart-refresh pass on 2026-04-08 now fingerprints render-relevant frontend state, skips polling rerenders when nothing changed, only fetches niche-project data on routes that actually render it, keeps legacy `pipeline-board` episode polling disabled because that hash is redirect-only, and slows refresh cadence to `10s` idle / `3s` active
  - CLI provider probe calls are now cached briefly inside `CliRunner`, avoiding repeated `codex`/`claude` subprocess checks on every click
- **AI agent configs** (`config/agents/`) — scene planning, visual bible, video/image prompts (Claude + OpenAI)
- **Video assembly subpackage** (`tool1_dashboard/video_assembly/`) — Tool 2 rendering core ported into Tool 1 on 2026-04-05
  - Imported modules: pipeline, timeline loading, validation, asset resolution, probing, image/video scene rendering, concat, mux, subtitle burn, FFmpeg helpers, shared utils, exceptions
  - Portability follow-up: the copied Tool 2 utils were adjusted to use `datetime.timezone.utc` so `RenderPipeline` imports cleanly on the repo's Python 3.10 runtime
- **FFmpeg readiness visibility** — `/api/health` now exposes `ffmpeg_available`, populated at app startup via `shutil.which("ffmpeg")` + `shutil.which("ffprobe")`
- **Video assembly database groundwork** — `tool1_dashboard/database.py` now creates and exposes CRUD for `render_jobs`, `scene_assets`, and `render_logs`; episode/project deletion now clears those records explicitly so the upcoming Tool 2 integration stages can build on stable per-language render state and shared asset tracking
- **Video assembly stage config groundwork** — `tool1_dashboard/config.py` now extends `EPISODE_PIPELINE_STAGES` with `asset_upload`, `assembly_validation`, `video_render`, and `final_review`, and defines `VIDEO_ASSEMBLY_STAGES = ("asset_upload", "assembly_validation", "video_render")` so backend/frontend workflow wiring can target the post-export Tool 2 stages without making them runnable provider stages
- **Video assembly Kanban groundwork** — `tool1_dashboard/ui/app.js` now exposes the four post-export Tool 2 columns in `EPISODE_PIPELINE_COLUMNS` and keeps them out of `EPISODE_RUNNABLE_STAGE_IDS`, matching the backend config separation between provider-run stages and assembly UI stages
- **Video assembly asset-upload backend** — `tool1_dashboard/service.py` now supports per-language assembly project prep plus shared-asset listing/upload/bulk-match/delete/preview flows, and `tool1_dashboard/app.py` exposes the Phase 3 API routes under `/api/episodes/{id}/scenes...`
- **Drawbridge bulk-upload repair on 2026-04-07** — shared asset uploads now enforce the scene's expected asset type, bulk uploads understand Windows batch names like `img (1)` and `video (1)` as separate image/video sequences instead of one mixed numeric stream, and the asset picker inputs now advertise `image/*,video/*` with clearer in-app guidance so mixed episodes no longer silently attach images to video scenes or skip the whole video batch
- **Drawbridge assembly stability follow-up on 2026-04-07** — episode detail and overlay now pre-render the assembly/review shells from frontend cache so background `renderApp()` refreshes no longer blank either section before hydration, and assembly file pickers now suspend auto-refresh plus bind direct file-input change listeners so browser file selections survive rerenders instead of dying before `bulk-upload` / single-upload requests fire
- **Lightweight assembly upload execution on 2026-04-08** — `tool1_dashboard/ui/app.js` now paginates assembly scenes 20 at a time with per-episode visible counts, keeps the existing lazy video placeholder renderer in a shared `renderSceneCardHtml(...)` path, derives assembly read-only state from the mounted stage, and updates only the affected scene card after single upload/delete/replace instead of rerendering the full section. Bulk uploads now run in sequential 5-file batches with `batch X/N` progress notices plus one final rerender. `tool1_dashboard/service.py` now exposes a shared scene serializer plus `get_single_scene(...)`, and `tool1_dashboard/app.py` now exposes `GET /api/episodes/{episode_id}/scenes/{scene_id}` for incremental refreshes. Verification passed with `node --check tool1_dashboard/ui/app.js`, `python -m pytest tests/test_video_pipeline.py -q` (`72` passing), and a live browser smoke on episode `ep-20260402-201657-205` confirming the assembly grid loads `20` cards first, expands to `40` via `Show 20 more`, and stays stable in `asset_upload`.
- **Video assembly validation backend** — `tool1_dashboard/service.py` now validates shared scene coverage plus per-language timeline/TTS/SRT prerequisites, and controls assembly stage transitions through `/api/episodes/{id}/assembly/start`, `/assembly/validate`, and `/assembly/advance`
- **Video assembly render backend** — `tool1_dashboard/service.py` now stages shared assets into per-language `assembly/{lang}/input/assets/`, creates queued `render_jobs`, runs single-language or `all` render batches sequentially through a process-wide render lock, and serializes per-language job status/output metadata; `tool1_dashboard/app.py` now exposes `/assembly/render`, `/assembly/render-status`, `/assembly/render-jobs`, `/assembly/render/{job}/video`, and `/assembly/render/{job}/scene/{scene_id}`
- **Video assembly safety backend** — `tool1_dashboard/service.py` now blocks `validate_assembly()` and `start_render()` when `ffmpeg`/`ffprobe` are unavailable, rejects new renders while the shared TTS worker heartbeat reports `processing`, auto-removes per-language `temp/scenes/` after successful renders, supports render-job deletion with temp cleanup, and supports episode-wide assembly temp cleanup; `tool1_dashboard/app.py` now exposes `POST /api/episodes/{id}/assembly/cleanup` and `DELETE /api/episodes/{id}/assembly/render/{job_id}`
- **Video assembly conditional frontend loader** — `tool1_dashboard/ui/app.js` now calls `loadAssemblyUI(episodeId)` from the episode supplemental-data path, hides the assembly surface before the post-export assembly stages, shows upload/read-only assets during `asset_upload`/`assembly_validation`, switches to per-language render progress during `video_render`, and shows completed-video review cards during `final_review`, while normalizing the current backend payloads for validation/render/SSE events
- **Video assembly encoding pass on 2026-04-08** — concat now stream-copies the already normalized scene clips instead of re-encoding them, scene renders now use `libx264 -preset fast -crf 20`, subtitle burn now uses `-preset fast`, and regression coverage now includes explicit ffmpeg-argument checks plus a real local FFmpeg smoke for mixed-assets/no-subtitle/subtitle render paths
- **Assembly probe-cache pass on 2026-04-08** — `_stage_assets_for_render()` now writes `input/cached_probes.json` keyed by the render timeline `asset_id` format (`asset_{index:03d}` for flat-array timelines, explicit `asset_id` when present), and `tool1_dashboard/video_assembly/probe_assets.py` now reuses complete width/height/duration metadata from that cache before falling back to `ffprobe` for incomplete entries, removing redundant re-probing on fresh uploads while preserving the safe fallback path for older assets
- **Elapsed timer lifecycle fix on 2026-04-08** — `tool1_dashboard/ui/app.js` now keeps a single shared `running-elapsed` interval alive across rerenders instead of calling `clearInterval(...)` and recreating it on every `renderApp()` pass, which removes the repeated timer teardown/restart churn from passive refresh cycles while preserving the per-second DOM update behavior
- **Assembly SSE log-growth fix on 2026-04-08** — `tool1_dashboard/ui/app.js` now batches render-log appends through a `DocumentFragment`, clears the `Waiting for logs...` placeholder on first real SSE payload, caps the DOM-backed log output to the newest `300` text nodes, and keeps the auto-scroll write to one post-batch update so long-running assembly renders stop growing the overlay log without bound
- **Assembly SSE lifecycle fix on 2026-04-08** — `tool1_dashboard/ui/app.js` now keeps render progress streams on the backend's explicit `event: update` channel, closes every open render `EventSource` when the user switches episodes or leaves the episode view, blocks delayed SSE starts after navigation, and drops stale connections after 5 minutes of inactivity instead of letting browser auto-reconnect loops linger forever
- **Drawbridge assembly scroll-glitch repair on 2026-04-07** — passive dashboard refreshes now reuse the existing assembly section when the user is still on the same episode/stage, so the `#episode-assembly-section` no longer flashes a transient loading surface or replaces in-view content during background polling; passive refresh failures now keep the current assembly UI mounted and raise a notice instead of blowing away the scrolled section
- **Video assembly unit coverage** — `tests/test_video_assembly/` now covers flat-array timeline conversion, project validation, asset filename/type matching, and `DashboardRenderObserver` writes into `render_jobs` / `render_logs`
- **Test suite** — 239 tests passing, 4 subtests passing (`python -m pytest tests -q` on 2026-04-05)

### What's Being Worked On

**Status as of 2026-04-05:** The original 10-phase Tool 1 rebuild is complete, and Tool 2 integration is in progress under `IMPLEMENTATION_PLAN_TOOL2_INTEGRATION.md`. Backend Phases 0-5 are done, and the frontend now has the conditional assembly loader for the post-export stages; remaining work is polish/safety (`10.x`) plus final integration testing (`11.x`).

Production subtitle metrics (episode 205 benchmark):
- DE: 4 warnings / 746 segments, max 23.4 CPS — excellent
- ES: 2 warnings / 687 segments, max 20.3 CPS — excellent
- FR: 23 warnings / 723 segments, max 25.1 CPS — good (3.2% slightly fast, zero segments over 30 CPS)
- IT: 20 warnings / 695 segments, max 23.9 CPS — good (zero segments over 24 CPS)

Remaining French/Italian density is inherent to the text-to-audio ratio for those languages, not a segmentation deficiency. If future episodes need further improvement, the fix is upstream (shorter translations or slower TTS pacing), not more segmentation tuning.

### What Is Still Fragile
- No dedicated frontend test harness yet; browser verification is still manual/smoke based
- Intermittent `/api/board` 404 noise appeared in local smoke logs, but no current source call was found in `tool1_dashboard/ui/app.js`
- Live translation-model discovery depends on a valid OpenAI API key; local browser smoke still uses a mocked discovery route when a real key is unavailable
- Live workflow smoke can fail immediately when Claude quota is exhausted; on 2026-03-27 the provider returned `Claude limit reached. You've hit your limit · resets Mar 28, 5pm (America/Sao_Paulo)`, and the frontend now surfaces that inline correctly but cannot resolve the quota issue itself
- French/Italian subtitle density is accepted at current levels (FR 23/723 warnings = 3.2%, IT 20/695 = 2.9%). Zero segments over 30 CPS. Any future density improvements should target upstream translation length or TTS pacing, not segmentation tuning.
- The new translation QA is intentionally stricter than the previous pipeline: weak translation profiles now fail closed instead of shipping awkward literal phrasing. Episode `205` French already forced one operational upgrade from `openai / gpt-5-nano` to `openai / gpt-5.4-mini`, and similar borderline language profiles may need the same treatment.
- Reviewer outputs from the LLM judge can still be noisy, but the new sanity filter now rejects unsupported source-channel complaints and minor contradictory failures; any future French work should focus on alignment behavior, not translation acceptance
- The project board still relies on manual/smoke browser verification for live workflow transitions; the inline status row and active polling are now in place, but there is still no automated frontend regression suite covering draft->queued/running movement
- XTTS runtime availability still matters operationally, but the UI no longer asks the user to manually start or restart the worker
- Legacy paused-TTS rows created before the 2026-03-28 recovery pass should self-heal on the next reconciliation tick, but there is still no one-off migration/reporting command for auditing older stuck records in bulk
- Fresh Windows environments still need the XTTS runtime installed manually; long-form throughput is only acceptable when the dashboard Python environment uses the CUDA build of the pinned `torch` / `torchaudio`, and Coqui TTS may still require Microsoft C++ Build Tools before voice cloning can work

### Git State
- Branch: `codex/tool2-phase5-render` (active local branch for Tool 2 Phase 5 render work on 2026-04-05)
- Previous feature branches: `codex/french-first-subtitle-cleanup`, `fix/italian-alignment-retry`, `codex/tool2-phase0`, `codex/tool2-phase1-db-schema`, `codex/tool2-phase2-stage-config`, `codex/tool2-phase3-assets` — merged or superseded by current Tool 2 integration work
- Remote: `https://github.com/alesalesmota/tool-1-srt-timeline-json-prompt-list.git`

## Architecture Decisions

| Decision | Why | Date |
|----------|-----|------|
| TTS only, no human audio upload | All narration generated via TTS for full automation | 2026-03-24 |
| One master language defines scene structure | Enables shared assets across all languages | 2026-03-24 |
| Duration mismatch MVP: images stretch, videos hold last frame | Keep MVP simple | 2026-03-24 |
| Pre-configured voice/translation profiles per Niche Project | Minimize per-video setup | 2026-03-24 |
| Sequential processing (no parallel TTS/translation) | Local machine constraints, GPU-bound TTS | 2026-03-24 |
| Consistency guide per-episode (not per-niche) | More flexibility per episode | 2026-03-25 |
| Remove all legacy code (Jobs, Projects/Builds) | Episodes is the final model, legacy is ~4000 lines of dead weight | 2026-03-25 |
| Archive outdated docs instead of deleting | Preserves reference material | 2026-03-25 |
| Project board is the primary workflow surface | The user works project-first, not from a global board | 2026-03-26 |
| Creating an episode only creates a Draft card | Queueing must be explicit and user-controlled | 2026-03-26 |
| Queue/requeue is blocked by server-side readiness checks | Missing voices, translations, provider auth, or languages must fail fast before work starts | 2026-03-26 |
| Provider failures stay on the failed stage with full logs | Failures must be actionable; no silent Claude->Codex fallback | 2026-03-26 |
| Template/settings reads are side-effect free | GET requests should not mutate template state | 2026-03-26 |
| Voice profiles are language-agnostic | XTTS profiles can be reused across languages, so the UI should not force or validate a language tag | 2026-03-26 |
| Voice engine lifecycle is automatic and demand-driven | Users should trigger voice actions, not manage worker processes; interactive work can cool down quickly while pipeline TTS stays warm until the queue drains | 2026-03-26 |
| Narration pacing is tuned per voice profile and snapped into each TTS job | Different voices need different stability/variation bands, and queued jobs must stay reproducible even if the profile is edited later | 2026-03-26 |
| App-side TTS chunking is authoritative; XTTS internal splitting stays off | Long-form narration must be resumable and predictable, so the repo controls chunk boundaries instead of leaving them to model-side splitting | 2026-03-26 |
| Translation Profiles use a dedicated provider-mode setup flow | Translation execution currently runs through API providers, so the setup UI must distinguish runnable OpenAI API profiles from future CLI preview modes instead of reusing the stage-provider catalog | 2026-03-26 |
| Stale provider stage-runs fail automatically after a timeout | Provider-driven stages must stop in an actionable retry state instead of appearing to run forever when the external CLI stalls or never returns output | 2026-03-27 |
| Workflow-stage model fields use select dropdowns from MODEL_CATALOG and OpenAI stages share one saved API key | Model selection should be discoverable via dropdown, not free-text; OpenAI-backed scene/style/prompt stages reuse one settings-level key | 2026-03-29 |
| Codex CLI removed; both `codex` and `openai` providers route through OpenAI HTTP API | `codex exec` is an agentic tool that spawns agents with filesystem access, fundamentally wrong for pure structured JSON generation; the OpenAI Responses API is the correct path | 2026-03-29 |
| Manual workflow controls pause only at safe boundaries and selected-step reruns reset downstream outputs | Users need to resume or redo specific stages without losing valid upstream assets or silently restarting from `Consistency Guide` | 2026-03-28 |
| Stable project launch commands should also be persisted as Codex local-environment actions | The Codex app header Run button is part of the deliverable for local-app projects, so repo-local environment config should carry the canonical launch command instead of relying on chat-only instructions | 2026-03-28 |
| Episode output-file browsing must preserve list scroll state across auto-sync rerenders | The file panel refreshes live while the episode detail stays open, so nested listboxes cannot snap users back to the top every time the DOM refreshes | 2026-03-28 |
| Scene-planning chunk validation trims a fully malformed trailing suffix instead of failing the whole episode | Overlapping chunk ownership already lets neighboring chunks cover the edge, so zero/negative-duration LLM spillover at the very end should be dropped with warnings rather than blocking the entire timeline stage | 2026-04-03 |
| Saved voice-profile chunk size is authoritative for both preview and production TTS, capped at an XTTS-safe ceiling | Live XTTS warnings on episode `205` showed the old production-only `260`-char floor could cross the model's practical limit and risk truncated narration | 2026-04-03 |
| Per-language retry must cover downstream repairable steps, and skipped timeline rows must not overwrite the real upstream failure | A finished multilingual workflow still needs actionable single-language recovery; masking an alignment failure as `No SRT available for timing` hides the real fix and blocks targeted reruns | 2026-04-03 |
| Worker health must expose runtime device/build metadata and queue depth | When TTS feels stuck, the user needs to tell the difference between CPU slowness, a real worker failure, and queued work behind the active language | 2026-03-29 |
| Desktop app window owns the backend lifecycle | A normal user cannot be expected to hunt for hidden Python/TTS processes in Task Manager; closing the visible app window must stop the dashboard and its XTTS worker, and duplicate app launches must be blocked at the launcher level | 2026-04-03 |
| Video assembly state lives in dedicated per-scope SQLite tables (`scene_assets`, `render_jobs`, `render_logs`) | Shared uploaded assets and per-language render history have different lifecycles, so the DB must track them separately and preserve rerender history without duplicating asset ownership | 2026-04-05 |
| Shared video-assembly assets are stored once under `{episode_workspace}/assembly/shared_assets/`, while per-language assembly inputs are materialized on demand under `{episode_workspace}/assembly/{lang}/input/` | The same media set must be reused across every language render, but Tool 2 still expects each render to run from a language-specific project directory | 2026-04-05 |
| Post-export assembly stages are user-driven and keep the episode in a paused workflow state until an explicit render starts | Asset upload, validation, and final review are interactive steps, so they must appear in the pipeline without being treated as auto-runnable provider stages | 2026-04-05 |
| Final video renders are serialized through one process-wide render lock, and `Render All` expands into one queued `render_jobs` row per valid language | The local machine cannot sustain multiple Tool 2 renders at once, so the dashboard must preserve per-language history while still enforcing one render pipeline at a time | 2026-04-05 |

## User Observations & Insights

- **2026-04-04**: Per-language YouTube channel names are intentional product behavior, not translation drift; when a source script mentions the channel name, the translation layer should replace it with the configured localized name (`Biblo Viral` for ES, `Orizzonte` for IT, etc.) while still translating the surrounding CTA text fully into the target language
- **2026-04-04**: Old concept/spec files left at the repo root can mislead future AI work; when a document no longer matches the shipped app, archive it or mark it explicitly historical instead of leaving it to look current
- **2026-04-04**: For live repair work, go one issue at a time and verify after each stage so the fix does not drift into mistakes or hallucinated changes
- **2026-04-07**: The assembly bulk-upload flow must work for image batches and video batches in the same mixed timeline; matching only by raw scene number is a real production bug because the user renames files in Windows as `img (...)` and `video (...)` and expects those sequences to map within each asset type
- **2026-04-04**: An idle terminal does not mean the TTS worker is hung; when a long narration run is active in the background, the user needs a reliable way to tell the difference between real progress and a stuck agent without hunting through Task Manager
- **2026-04-02**: A bridge-reported `TTS / Voice` failure can actually be an upstream translation problem; empty translated-script artifacts must not be treated as valid output, and the retry path should bring the episode back to `translation` instead of leaving a misleading empty-audio error on the TTS step
- **2026-04-03**: Repeated XTTS `character limit of 250` warnings during live narration are a real production bug signal, not harmless noise; when those warnings appear in a batch run, stop the job, fix the chunk sizing, and regenerate instead of letting a potentially truncated narration finish
- **2026-04-03**: TTS must stay strictly one language at a time; if two narration jobs run in parallel the machine load is unacceptable, so duplicate workers should be treated as a production bug and collapsed back to a single queue consumer immediately
- **2026-04-03**: The app cannot behave like a browser tab with hidden Python processes behind it; the user needs one visible desktop window, a clear place inside the app to see whether the backend/voice engine are running, guaranteed single-instance behavior, and closing that window must stop all background work without requiring terminals or Task Manager
- **2026-04-03**: Scene planning should stop asking the model to invent raw `start/end` timings when a precise SRT already exists; the model should focus on contextual scene grouping only, and the backend should derive deterministic timings from subtitle anchors instead of fuzzy text matching
- **2026-04-03**: When one language finishes the workflow with a hidden upstream failure, the UI must show the real failing step and allow retrying only that language from the broken stage onward instead of forcing a full rerun
- **2026-03-26**: The project page should be the real workspace: project Kanban first, not a flat episode list and not a global board
- **2026-03-26**: Adding an episode should leave it in Draft; queueing must be explicit instead of automatic
- **2026-03-26**: The primary project CTA should say `Create episode`; the Draft state is secondary detail and should be explained in helper copy instead of the main button label
- **2026-03-26**: In the Draft column, the add action can be just a `+`; the explanation should appear on hover instead of taking permanent header space
- **2026-03-26**: Column explanations should appear when hovering the column title instead of staying visible and occupying board space
- **2026-03-26**: Target-language selection in the create-project modal should be a searchable picker that adds languages into a list below, not a large checkbox grid
- **2026-03-26**: Episode details should open as an overlay on top of the project board, not force a full navigation away from the workflow
- **2026-03-26**: Queueing and requeueing should be blocked when setup is incomplete (missing voice profiles, translation profiles, provider login, or languages)
- **2026-03-26**: Provider failures must remain explicit and controllable; do not add automatic Claude->Codex fallback
- **2026-03-26**: App feedback feels too slow; clicking pages or any action should respond immediately instead of feeling delayed by background refresh work
- **2026-03-26**: Voice clone/test feedback must stay inside each voice-profile card; the global worker badge alone is too abstract, `stale` is unclear copy, and the user needs an inline way to hear the generated preview clip
- **2026-03-26**: Voice profile creation should only ask for a name and an audio file; language should not be required because these XTTS voices can handle multiple languages
- **2026-03-26**: The voice-profile card should be much simpler, show less technical information, and use one click to generate a fresh test sample instead of asking for manual test text
- **2026-03-26**: A manual `Start` button does not make sense for voice cloning/TTS; the app should start and stop the voice engine intelligently based on actual usage
- **2026-03-26**: `Needs restart` is misleading copy when the user is actively inside the app; normal sleeping/offline engine state should not be presented as a user task
- **2026-03-26**: Voice rhythm can vary between generations, but production narration needs tighter pacing guardrails; very slow takes feel unreal and retries become too expensive on long workflows
- **2026-03-26**: Automatic quality retries are not desirable for long-form narration because they add too much runtime; the system should keep natural variation but constrain it to a believable rhythm band and expose manual tuning when presets are not enough
- **2026-03-26**: Long-form narration should not rely on XTTS internal text splitting; explicit app-side TTS chunking should stay in control so resumability and pacing are predictable, and voice tuning should live on each voice profile rather than in global settings
- **2026-03-26**: The voice-profile play-testing controls should stay minimal across all states; play, tuning, and delete should read as compact icon actions with hover explanations instead of bulky text buttons
- **2026-03-26**: Translation profile setup needs a clear distinction between CLI use and API use; if an API key is pasted the app should recognize it, show the available models, and let the user sort/filter them with simple relative cost/speed guidance shown on hover
- **2026-03-27**: Project setup disclosures cannot auto-close during background refresh; the user must be able to open `Language setup`, interact with dropdowns, and keep working without the page collapsing the section underneath them
- **2026-03-28**: The `Episode output files` list must scroll independently; auto-sync should never push the listbox back to the top while the user is browsing long artifact lists
- **2026-03-27**: Translation-profile cards should stay compact in the default view; the page should not repeat `Translation Profiles`, the card should only show language/provider/model/readiness, and deeper details should appear only after clicking the card
- **2026-03-27**: The two global shell controls in the top-right corner can live in the lateral menu instead; the topbar should stay focused on the current page title and notices
- **2026-03-27**: Long-running provider stages like `Consistency Guide` cannot look infinite; if the CLI stalls or takes too long to produce the style guide, the app should fail the run with a clear timeout message and leave the episode in a retryable state
- **2026-03-27**: `Queue` is unclear on the episode workflow surface; the action should say `Start workflow` / `Restart workflow` / `Run again`, and the controls should stay minimal icon buttons with hover explanations instead of large permanent text buttons
- **2026-03-28**: Restarting from the TTS step must not jump all the way back to `Consistency Guide`; the user needs a workflow interface that can start, pause, resume, and rerun from specific steps depending on which assets already exist
- **2026-03-28**: In the episode output-files area, a manual refresh button does not make sense; files should appear live, clicking any file must show its contents immediately, and the UI has to make it obvious which outputs contain data versus which ones are still empty
- **2026-03-27**: After pressing play on the project Kanban, feedback felt too slow and too hidden; the card should show a live minimal status explaining what the backend is doing, and a started card cannot remain visually stuck in the `Draft` column
- **2026-03-28**: When an episode is paused in `TTS / Voice`, the card cannot look dead; the user needs a visible signal that XTTS is actively generating audio, including which language is running and how far through the chunk batch it is
- **2026-03-28**: In multilingual TTS, the UI must distinguish the one language that is actively generating from the remaining languages that are only queued; otherwise it looks like multiple voices are running at once and overloading the machine
- **2026-03-28**: The episode overlay sidebar is confusing because it duplicates controls and information already shown in the main section; the modal should stay focused on one main flow instead of splitting quick actions and run history into a separate side panel
- **2026-03-28**: Workflow state must reflect real backend work; after restarting the app, a card cannot continue to look `running` for a long time if no job is actually progressing, so the system needs a deeper audit and explicit recovery/failure behavior across the workflow
- **2026-03-28**: Live activity cannot stop at a generic `running` message; the UI must say whether it is showing real stdout/stderr preview files or only a fallback execution snapshot, and it must show when those preview files were last updated
- **2026-03-28**: When a repo has a stable local launch command, Codex should set the app Run button itself instead of only telling the user which terminal command to execute
- **2026-03-29**: The current TTS step is taking far longer than the preview TTS tool; the user wants the real architecture understood deeply, why the current production path is slower than preview, and fixes that favor real throughput instead of just better status copy
- **2026-03-27**: Workflow-stage model options should refresh when providers evolve; API-backed providers should be able to scan current models from the pasted key, and CLI-backed stages should still allow manual model ids so the UI does not block newer releases
- **2026-03-27**: The scene/style/prompt provider setup should support a shared API-key path too, not only local CLI auth, so `Scene Planning`, `Consistency Guide`, and prompt stages can run through OpenAI when that is the better fit
- **2026-03-25**: Lost 10+ phase plan between conversations → created IMPLEMENTATION_PLAN.md + IMPLEMENTATION_CHECKLIST.md in repo + updated CLAUDE.md behavior to always persist plans
- **2026-03-25**: Standalone tools (TRADUTOR, TTS, SRT chunker, Whisper UI) all duplicated integrated modules → deleted
- **2026-03-24**: Niche Project hierarchy — each niche has pre-configured languages, voice profiles, translation profiles
- **2026-03-24**: Future: multiple Niche Projects (Religion, Sports, etc.)

## Future Improvements

- Add automated browser regression coverage for the project board and episode overlay workflow
- Trace and eliminate the stray `/api/board` 404 log source if it reappears in future smoke runs
- Add richer provider health diagnostics so readiness can distinguish login, quota, and binary availability more precisely
- Detect CLI model catalogs automatically if/when `claude` exposes a stable model-list command, so the model select dropdowns can refresh from the installed tool instead of relying on curated defaults
- Expand Translation Profiles beyond OpenAI once the CLI execution path and persistence contract for `Codex CLI` / `Claude Code CLI` are ready
- Add chunk-level narration diagnostics so long-form TTS can report pacing anomalies and per-chunk timings without introducing automatic retries
- Add a one-click runtime repair or diagnostics script for the dashboard Python environment so CUDA-vs-CPU state can be repaired or audited without manual pip commands
- Consider carrying `last_completed_chunk` in job metadata so the UI can hold 100% during the final merge step without relying on progress-string parsing
- Add a clearer “production safe” vs “more expressive” explanation in the tuning modal so preset choice is more obvious before users touch the advanced controls
- Consider inline stage-run diffing/retry tools in the episode overlay once the current workflow remains stable
- Refactor `scene_planning` into an anchor-based flow where the model returns scene groups tied to master subtitle cue ids or cue ranges, and the backend computes `start/end/duration/text` deterministically from the SRT
- Keep cross-language timing deterministic by reusing cue indices or a cue-map artifact during `timeline_mapping`, instead of trying to rediscover translated scene text by similarity search

## Phase Plan

**See `IMPLEMENTATION_PLAN.md` for full details and `IMPLEMENTATION_CHECKLIST.md` for progress.**

| Phase | Goal | Status |
|-------|------|--------|
| Pre-Implementation | Continuity setup (plan docs, checklist, CLAUDE.md) | DONE |
| Phase 1 | Cleanup & Git Hygiene | DONE |
| Phase 2 | Database Consolidation | DONE |
| Phase 3 | Service Layer — Remove Legacy | DONE |
| Phase 4 | API Layer Consolidation | DONE |
| Phase 5 | Frontend — Remove Legacy Views | DONE |
| Phase 6 | Episode Pipeline Board Enhancement | DONE |
| Phase 7 | Niche Project Detail Enhancement | DONE |
| Phase 8 | TTS & Translation Polish | DONE |
| Phase 9 | Review & Export Phase | DONE |
| Phase 10 | Final Cleanup & Documentation | DONE |

## Change Log

| Date | What Changed |
|------|-------------|
| 2026-04-08 | **B2 probe-cache render optimization shipped**: updated `tool1_dashboard/service.py` so `_stage_assets_for_render()` parses the copied render timeline, stages shared assets in timeline order, and writes `input/cached_probes.json` using the same `asset_id` keys that `timeline.py` resolves at render time. Updated `tool1_dashboard/video_assembly/probe_assets.py` to read that cache, reuse complete metadata without calling `ffprobe`, and fall back to real probing when cached entries are incomplete. Added regression coverage in `tests/test_video_assembly/test_probe_assets.py` plus a service test in `tests/test_video_pipeline.py` to verify the cached-probe key mapping. Verification passed with `python -m pytest tests/test_video_assembly/test_probe_assets.py -q` (`2` passing), `python -m pytest tests/test_video_pipeline.py -k "prepare_assembly_project_copies_language_specific_inputs or stage_assets_for_render_writes_cached_probes_with_timeline_asset_ids" -q` (`2` passing), and `python -m pytest tests/test_video_assembly_integration.py -q` (`16` passing). |
| 2026-04-08 | **F4 elapsed-timer fix shipped**: updated `tool1_dashboard/ui/app.js` so `resetElapsedTimer()` becomes start-once and no longer clears/recreates the global timer on every rerender. This keeps the existing `querySelectorAll(".running-elapsed")` tick logic intact while removing repeated interval teardown during dashboard refreshes. Updated `plans/performance/PERFORMANCE_CHECKLIST.md` and `plans/performance/PERF_PLAN_HIGH_FINDINGS.md` to mark F4 complete. Verification passed with `node --check tool1_dashboard/ui/app.js` plus an inline Node smoke that confirmed a single interval registration across repeated calls and preserved elapsed text updates (`elapsed-timer-start-once-ok`). |
| 2026-04-08 | **F5 SSE log-growth fix shipped**: updated `tool1_dashboard/ui/app.js` so assembly render logs append through a shared `appendRenderLogMessages(...)` helper, clear the initial `Waiting for logs...` placeholder on first data, batch new lines through `DocumentFragment`, and trim the log DOM back to the newest `300` entries after each SSE batch. Updated `plans/performance/PERFORMANCE_CHECKLIST.md` and `plans/performance/PERF_PLAN_HIGH_FINDINGS.md` to mark F5 complete. Verification passed with `node --check tool1_dashboard/ui/app.js` plus an inline Node smoke that exercised placeholder clearing, capped growth, and batched append behavior (`render-log-helper-ok`). |
| 2026-04-08 | **F6 SSE lifecycle fix shipped**: updated `tool1_dashboard/ui/app.js` so render progress only listens on the backend's named `update` events, render SSE connections are centrally tracked with per-job inactivity timers, route/episode transitions close every open `EventSource`, and delayed `startRenderSSE(...)` calls now bail out if the episode view is no longer active. Added a regression test in `tests/test_video_pipeline.py` to verify the backend stream emits `event: update`, updated the performance checklist, and moved the finished high-priority plan to `plans/completed/PERF_PLAN_HIGH_FINDINGS.md`. Verification passed with `node --check tool1_dashboard/ui/app.js`, `python -m pytest tests/test_video_pipeline.py -k "render_job_events_endpoint_streams_named_update_events" -q` (`1` passing), and an inline Node smoke covering the lifecycle wiring (`render-sse-lifecycle-ok`). |
| 2026-04-08 | **Video encoding performance plan executed**: updated `tool1_dashboard/video_assembly/concat_scenes.py` to use `-c:v copy` for concat, added `-preset fast -crf 20` to both scene renderers, changed subtitle burn from `medium` to `fast`, and added targeted regression coverage in `tests/test_video_assembly/test_encoding_commands.py`. Verification passed with `python -m pytest tests/test_video_assembly/test_encoding_commands.py -q` (`4` passing), `python -m pytest tests/test_video_assembly_integration.py -q` (`16` passing), plus a real local FFmpeg smoke that rendered mixed image/video scenes through concat, mux, and subtitle burn. The completed plan was moved to `plans/completed/PERF_PLAN_VIDEO_ENCODING.md`. |
| 2026-04-08 | **Plans folder cleanup**: created `plans/completed/` as the home for finished implementation plans and moved the completed performance plans (`ASSEMBLY_LIGHTWEIGH_PLAN.md`, `PERF_PLAN_SMART_REFRESH.md`) out of the active `plans/performance/` queue so future work stays focused on unfinished items. Updated the performance checklist and README to point at the new completed-plan paths. |
| 2026-04-08 | **Smart refresh plan executed**: updated `tool1_dashboard/ui/app.js` so passive refreshes compute a render fingerprint and skip `renderApp()` when the rendered state is unchanged, made `/api/niche-projects` route-aware instead of fetching it on every view, confirmed `#/pipeline-board` is redirect-only and kept `/api/board/episodes` polling disabled, and increased the dashboard refresh cadence to `10s` idle / `3s` active. Evaluated `AbortController` but left it out because `state.isRefreshingData` already blocks overlapping refresh cycles in the current flow. Verification passed with `node --check tool1_dashboard/ui/app.js` and `python -m pytest tests/test_video_pipeline.py -q` (`72` passing). |
| 2026-04-08 | **Lightweight assembly upload plan executed**: added per-episode assembly pagination (`20` cards at a time plus `Show 20 more`), extracted a shared scene-card renderer that preserves the lazy video placeholder behavior, added `GET /api/episodes/{episode_id}/scenes/{scene_id}` with the same serialized shape as the bulk list endpoint, switched single upload/delete/replace to an incremental one-card refresh path, and rewrote bulk upload into sequential 5-file batches with progress notices plus a final section rerender. Extended `tests/test_video_pipeline.py` to cover the new single-scene endpoint before upload, after upload, after delete, and for missing scenes. Verification passed with `node --check tool1_dashboard/ui/app.js`, `python -m pytest tests/test_video_pipeline.py -q` (`72` passing), and a live browser smoke on `http://127.0.0.1:8020/#/episodes/ep-20260402-201657-205` confirming the assembly grid advanced from `20` to `40` cards without a loading flash. |
| 2026-04-07 | **Drawbridge bulk-upload fix shipped**: hardened `tool1_dashboard/service.py` so single-file uploads reject image/video type mismatches, bulk upload now maps `img (...)` and `video (...)` batches against image-only and video-only scene sequences instead of the old mixed raw-number matcher, and `tool1_dashboard/ui/app.js` now limits asset pickers to media files while clarifying the Windows rename convention in the hint text. Regression coverage in `tests/test_video_pipeline.py` now covers mixed-type bulk uploads plus explicit single-upload mismatch rejection. Verification passed with `node --check tool1_dashboard/ui/app.js` and `python -m pytest tests/test_video_pipeline.py -q` (`72` passing). |
| 2026-04-07 | **Drawbridge assembly/upload follow-up shipped**: moved the assembly and review sections onto cached frontend shells so full episode rerenders no longer flash `Loading scenes` or temporarily blank the Phase 9 placeholder during `asset_upload`, and paused auto-refresh while assembly file pickers/uploads are active while also binding direct `change` listeners on the file inputs. Browser validation on live episode `ep-20260402-201657-205` confirmed both sections stayed stable across two 6-second refresh intervals, and a dummy unmatched file triggered `POST /api/episodes/ep-20260402-201657-205/scenes/bulk-upload` without mutating real assets. |
| 2026-04-07 | **Drawbridge assembly scroll-glitch fix shipped**: updated `tool1_dashboard/ui/app.js` so `loadAssemblyUI(...)` tracks the mounted episode/stage and skips reinserting the `Loading scenes` shell when a passive auto-refresh is just refreshing the same assembly surface. Same-stage refreshes now reuse the current DOM, and passive refresh failures keep the existing section mounted while raising a notice instead of replacing the user's in-view content mid-scroll. Verification passed with `node --check tool1_dashboard/ui/app.js`; a live browser repro was attempted, but the current local runtime no longer exposes episode `205` (`/api/episodes/205 -> 404`). |
| 2026-04-05 | **Tool 2 integration Phase 0 completed**: activated `IMPLEMENTATION_PLAN_TOOL2_INTEGRATION.md`, added startup FFmpeg/FFprobe detection to `tool1_dashboard/app.py` with `ffmpeg_available` exposed on `/api/health`, ported the 14 planned Tool 2 rendering modules into `tool1_dashboard/video_assembly/`, added a package init file, and added `jinja2>=3.1,<4.0` to `requirements.txt`. Import verification passed with `python -c "from tool1_dashboard.video_assembly.pipeline import RenderPipeline; print('OK')"` after a Python 3.10 compatibility fix in the copied utils module (`datetime.timezone.utc` instead of `datetime.UTC`). Full regression verification passed with `python -m pytest tests/ -q` (`220` passing, `4` subtests passing). |
| 2026-04-05 | **Tool 2 integration Phase 1 completed**: added SQLite schema + CRUD support in `tool1_dashboard/database.py` for `render_jobs`, `scene_assets`, and `render_logs`, wired explicit cleanup of those records into episode/project deletion flows, and verified the new DB layer with a temporary-database smoke script plus full regression coverage via `python -m pytest tests/ -q` (`220` passing, `4` subtests passing). |
| 2026-04-05 | **Tool 2 integration tasks 2.1-2.2 completed**: extended `tool1_dashboard/config.py` so the episode pipeline now continues past `export` into `asset_upload`, `assembly_validation`, `video_render`, and `final_review`, added `VIDEO_ASSEMBLY_STAGES` for the assembly-only workflow subset, and updated `tool1_dashboard/ui/app.js` so the Kanban model exposes the same four post-export Tool 2 columns while keeping them out of runnable provider-stage controls. |
| 2026-04-05 | **Tool 2 integration Phase 3 completed**: added `_prepare_assembly_project()` plus shared asset management methods in `tool1_dashboard/service.py`, exposed `GET /api/episodes/{id}/scenes`, single upload, bulk upload, delete, and preview endpoints in `tool1_dashboard/app.py`, and added regression coverage for the new asset API + assembly project prep path in `tests/test_video_pipeline.py`. Verification passed with `python -m pytest tests/test_video_pipeline.py -q` (`63` passing) and `python -m pytest tests/ -q` (`222` passing, `4` subtests passing). |
| 2026-04-05 | **Tool 2 integration Phase 4 completed**: added assembly validation logic in `tool1_dashboard/service.py` for shared scene assets plus per-language timeline/TTS/SRT prerequisites, exposed `/api/episodes/{id}/assembly/validate`, `/assembly/start`, and `/assembly/advance` in `tool1_dashboard/app.py`, and added regression coverage for validation reports and assembly-stage guards in `tests/test_video_pipeline.py`. Verification passed with `python -m pytest tests/test_video_pipeline.py -q` (`65` passing) and `python -m pytest tests/ -q` (`224` passing, `4` subtests passing). |
| 2026-04-05 | **Tool 2 integration Phase 5 backend completed**: added sequential render orchestration in `tool1_dashboard/service.py` with `_stage_assets_for_render()`, queued single-language or `all` render batches, process-wide render locking, per-job serialization helpers, and safe file serving for final videos plus per-scene clips; `tool1_dashboard/app.py` now exposes `/assembly/render`, `/assembly/render-status`, `/assembly/render-jobs`, `/assembly/render/{job}/video`, and `/assembly/render/{job}/scene/{scene_id}`; and `tests/test_video_pipeline.py` now covers concurrent-render blocking, render-all job creation, asset staging, render-status payloads, and output serving. Verification passed with `python -m pytest tests/test_video_pipeline.py -q` (`67` passing) and `python -m pytest tests/ -q` (`226` passing, `4` subtests passing). |
| 2026-04-05 | **Tool 2 integration task 8.3 completed**: added `loadAssemblyUI(episodeId)` wiring in `tool1_dashboard/ui/app.js`, so the episode detail/overlay now conditionally mounts the correct post-export assembly panel by stage (`asset_upload`, `assembly_validation`, `video_render`, `final_review`), hides the assembly section outside those stages, preserves a per-episode active render-language tab, normalizes the current backend validation/render payloads, and updates the render-all/render-SSE frontend wiring to match the backend API contract. Verification passed with `node --check tool1_dashboard/ui/app.js` and `python -m pytest tests/ -q` (`226` passing, `4` subtests passing). |
| 2026-04-05 | **Tool 2 integration Phase 10 Codex tasks completed**: added FFmpeg guards to assembly validation/render entrypoints, blocked new renders while the shared TTS worker is actively processing, auto-cleaned per-language `temp/scenes/` after successful renders, added `POST /api/episodes/{id}/assembly/cleanup` and `DELETE /api/episodes/{id}/assembly/render/{job_id}`, extended the DB/service layer with single render-job deletion, and updated render regression tests for the new cleanup semantics plus the new safety endpoints. Verification passed with `python -m pytest tests/test_video_pipeline.py -q` (`71` passing) and `python -m pytest tests/ -q` (`230` passing, `4` subtests passing). |
| 2026-04-05 | **Tool 2 integration task 11.2 completed**: added a dedicated `tests/test_video_assembly/` suite covering Tool 1 flat-array timeline conversion into Tool 2 config/scenes, project validation error/warning paths, asset resolver filename/type matching, and `DashboardRenderObserver` persistence into SQLite `render_jobs` and `render_logs`. Verification passed with `python -m pytest tests/test_video_assembly -q` (`9` passing) and `python -m pytest tests/ -q` (`239` passing, `4` subtests passing). |
| 2026-04-05 | **French-first subtitle cleanup shipped as a fast seeded repair path**: added `tool1_dashboard/alignment_tool/benchmark_segmentation.py`, allowed `segment_words(...)` to seed from an existing cue layout, tightened French/Italian subtitle break rules, added 3-block neighborhood re-segmentation plus pairwise silence-gap redistribution, and added alignment tests for the new repair/benchmark path. Local verification used `python -m unittest tests.test_alignment_tool -q` (`20` passing) because the available Python environment does not have `pytest` / `fastapi` / `httpx` installed. Non-destructive benchmark output was written to `workspace/benchmarks/alignment-20260405-fr-it-cleanup-all/summary.json`: `de=4`, `es=2`, `fr=23`, `it=20` reading-speed warnings. Severe outliers improved (`fr 5->2 over 24 cps, 1->0 over 30 cps`; `it 3->0 and 1->0`), but the total-warning gate is still red, so no live French/Italian rerun was promoted. |
| 2026-04-04 | **Subtitle-density hardening shipped at tool level**: replaced the old greedy subtitle splitter with a deterministic DP readability-first segmenter, added shared subtitle break rules to the language rulepacks, added timing-gap redistribution plus local repair attempts for dense cues, and extended `alignment_report.json` with subtitle-density diagnostics (`segment_profile`, `segments_over_18/24/30_cps`, fast-segment buckets, averages, optimization passes). Verified with `python -m pytest tests -q` (`214` passing, `4` subtests passing) and two non-destructive episode `205` benchmark runs using spoken-script inputs for `de/es/fr/it`. The current code benchmark in `workspace/benchmarks/alignment-20260404-density-v2/summary.json` landed at `de=2`, `es=2`, `fr=23`, `it=12` reading-speed warnings. French is dramatically improved versus the older `139`, but still misses the strict `>24` / `>30` cps acceptance gate, so no live French rerun was promoted yet. |
| 2026-04-04 | **Context-file cleanup for AI safety**: archived the outdated root Google Stitch application spec and the Tool 2 merge/integration guide because both described superseded concepts that no longer match the shipped app. Also marked `IMPLEMENTATION_PLAN.md` and `IMPLEMENTATION_CHECKLIST.md` as historical migration artifacts, and refreshed the current test commands in `README.md` / `docs/VALIDATION_NOTES.md` to point at the active `pytest` flow. |
| 2026-04-04 | **Tool-wide alignment hardening shipped and benchmarked against episode `205`**: upgraded the alignment stack to use language-aware spoken/token normalization, staged token rescue for merge/split/fuzzy mismatches, structured alignment candidate metrics in `alignment_report.json`, and subtitle post-optimization that extends segments into safe gaps and merges very short neighbors. Added a guided chunked-MFA retry strategy plus deterministic candidate scoring, while keeping `MFA` primary and `WhisperX` fallback. Verified with `python -m pytest tests -q` (`208` passing, `4` subtests passing) and a non-destructive benchmark on episode `205` using current narration assets: `de` improved from `15 -> 1` approximate words and `161 -> 105` reading-speed warnings, `es` from `2 -> 0` and `39 -> 12`, `fr` from `100 -> 4` and `311 -> 139`, and `it` from `8 -> 0` and `71 -> 57`. The live episode DB/review state was not mutated during benchmarking; results were written under `workspace/benchmarks/alignment-20260404/`. |
| 2026-04-04 | **French profile tune + reviewer-sanity fix completed**: tightened the reviewer prompt to require evidence from the translated script, normalized structured reviewer issues, pruned unsupported source-channel complaints, upgraded contradictory `passed = false` / score `>= 4` review results into pass-with-suggestions, and verified the behavior with two new translation-service tests. Live French candidate promotion for episode `205` then succeeded: the accepted `gpt-5.4-mini` French script was written to `script_fr.txt`, `translation_review_fr.json` now reports `5/5` across all criteria, the French translation profile for the project was updated to `gpt-5.4-mini`, and French TTS + alignment + timeline were regenerated from the accepted script. |
| 2026-04-04 | **Multilingual translation-quality framework implemented**: added shared language rulepacks (`en/de/es/fr/it`) for CTA/channel/reference guidance, deterministic chunk/script translation audits, a hybrid script-level reviewer gate on `gpt-5.4-mini`, readable + spoken script artifacts with `spoken_script_path` persisted in `episode_language_status`, TTS/alignment preference for spoken scripts, export packaging for both readable and spoken scripts, queue/TTS orchestration fixes that preserve direct helper semantics while showing paused-TTS episode jobs as active, and an OpenAI Responses API retry path for `max_output_tokens` truncation. Verified with `python -m pytest tests -q` (`198` passing, `4` subtests passing). |
| 2026-04-04 | **Episode `205` French regression now fails safely instead of shipping low-quality output**: live reruns proved the new deterministic validator blocks literal CTA phrasing like `Cliquez-la`, and the reviewer gate rejects the current French profile (`openai / gpt-5-nano`) for remaining fluency/naturalness issues. The previous French review artifacts were restored, `script_fr_spoken.txt` was generated, and the operational takeaway is that the stricter pipeline exposes weak translation profiles instead of masking them. |
| 2026-04-04 | **Episode `205` repair pass completed end-to-end**: hardened scene-planning prompts/merge validation so late chunk-local timestamps are rebased and cue-tail truncation becomes a hard failure, reprocessed the shared scene/prompt assets, and verified that `master_scenes.json`, `timeline_draft.json`, and `prompt_list_draft.txt` now all cover `378` scenes through the final English cue at `3361.6s` with `cue_tail_gap_seconds = 0.0`. |
| 2026-04-04 | **Translation retry path repaired for lossy scene text**: `build_scene_aware_chunks(...)` now accepts the full source script so translation retries preserve connective paragraphs that are absent from `master_scenes` text, translation audits now block mixed-language CTA leakage while preserving per-language channel names (`Biblo Viral`, `Orizzonte`, etc.), the OpenAI adapter now uses `reasoning = low` for `gpt-5.4*`, and episode `205` Spanish/Italian were rerun through translation -> TTS -> alignment -> timeline regeneration while French was preserved after the new QA rejected a fresh rerun. |
| 2026-04-04 | **Background XTTS worker health verified during a seemingly idle terminal**: confirmed that the shared narration worker was not stuck. The thread shell was already idle at a normal PowerShell prompt, while `python -m tool1_dashboard.tts.worker` kept running on CUDA in the background. Live verification showed the worker heartbeat advancing, `tts_jobs.progress` moving from `Generating chunk 307/358...` to `308/358...`, and the active job directory `workspace/tts/chunks/d596e093-85fa-4a91-8db9-5e0d7c4c369d/` growing from `612` to `616` chunk files in 10 seconds. Also recorded the operational caveat that `workspace/tts/worker.log` may appear stale because worker stdout is buffered. |
| 2026-04-03 | **Episode `205` export review failed the shared-asset completeness check**: manual verification of `workspace/episodes/niche-20260326-133703-religi-o/ep-20260402-201657-205` confirmed that all five language rows in `episode_language_status` are `done` and their script/audio/SRT/timeline files exist, but the shared review/export assets are truncated. `master_scenes.json`, `timeline_draft.json`, `prompt_list_draft.txt`, and `prompt_blueprint.jsonl` all end at `scene_382` / `2982.0s` (`the center of Christian faith.`) while `final_en.srt` continues to cue `904` / `3361.6s` and the localized SRTs run even longer. No export zip existed at review time, and the episode remained in `board_status = Review` / `pipeline_status = review`. |
| 2026-04-03 | **Italian alignment fix + single-language recovery complete**: corrected the Italian MFA model ids from the nonexistent `italian_mfa` to `italian_cv`, added backend/UI retry support for per-language `alignment` and `timeline_mapping`, preserved upstream alignment errors when timeline mapping skips for missing SRT, and verified the repair with targeted pytest coverage plus live single-language reruns that regenerated `final_it.srt` and `timeline_it.json` for episodes `204` and `205`. |
| 2026-04-03 | **Scene-planning malformed-tail recovery complete**: taught `tool1_dashboard/validators.py` to trim a fully malformed trailing suffix when the remaining LLM scene entries are only zero/negative-duration spillover or gap markers, added regression coverage in `tests/test_chunking_and_validation.py`, and replayed episode `205` through `scene_planning` on the patched code. The episode now has a regenerated `timeline_draft.json` and sits paused at `video_prompt_generation` instead of failing on `Scene 70 ends before it starts.` |
| 2026-04-03 | **Desktop launcher + runtime visibility pass complete**: replaced the old `webbrowser.open(...)` launcher path with a single-instance desktop shell based on `pywebview`, added `tool1_dashboard/launch_runtime.py` to own lock/runtime metadata/shutdown callbacks, updated the Settings page to show `App shell`, `App process`, and `Voice engine` runtime cards from the new `/api/app-runtime` endpoint, and switched the Windows batch launcher to `pythonw` so the normal user path is a visible app window instead of a console-backed browser tab. Live cleanup also stopped the previously hidden dashboard/XTTS processes, requeued the orphaned paused French TTS job for episode `205`, and relaunched the dashboard in `desktop` mode on `http://127.0.0.1:8020`. |
| 2026-04-03 | **TTS single-worker enforcement and live queue repair**: added DB helpers to list worker heartbeats, mark workers stopped, and requeue in-flight TTS jobs cleanly; updated `tool1_dashboard/tts/manager.py` so `ensure_worker_ready()` and `start_worker()` enforce a global singleton worker instead of trusting only the latest heartbeat row; and added regression coverage proving duplicate workers are terminated and their active language jobs are requeued. Live episode `205` was then normalized from an accidental two-worker state back to one worker and one active language, with the queue resumed under a single patched worker process. |
| 2026-04-03 | **TTS chunk inflation fix and live episode recovery**: removed the production-only `chunk_max_chars = max(profile, 260)` override from `tool1_dashboard/service.py`, lowered the manual voice-tuning ceiling from `260` to XTTS's safe `250`, updated TTS regression coverage so saved chunk sizes stay authoritative in both preview and production, and verified the pipeline-level queue builder against the fixed behavior. Live episode `205` was then recycled from the stale pre-fix TTS queue: the old `260`-char jobs were canceled, their partial chunk directories were cleared, fresh TTS jobs were requeued with `chunk_max_chars = 180`, the dashboard was restarted on the patched code, and the regenerated English/Spanish narration jobs resumed processing under `Running / paused_for_tts`. |
| 2026-04-02 | **Bridge translation-empty failure fixed**: traced the Drawbridge `TTS / Voice` failure on episode `205` to OpenAI `gpt-5-nano` translation calls that were returning `Responses API` outputs with reasoning only and no message text. Hardened `tool1_dashboard/translation/adapter.py` to request GPT-5 translations with minimal reasoning + low verbosity and to fail on `incomplete` / empty responses, hardened translation orchestration so blank outputs are recorded as failed chunks instead of `ok`, blocked TTS restarts when translated script files are empty, removed the unsafe non-master fallback to the English master script, added regression coverage for adapter/body handling plus blank-translation pipeline failures, and repaired the live episode state by canceling the orphaned English TTS job and moving episode `205` back to the `translation` stage with the empty translated-script rows cleared. |
| 2026-03-30 | **Intrinsic Design Specification created & refined**: Authored `APPLICATION_SPEC_FOR_GOOGLE_STITCH.md` at the project root. Added **Application Hierarchy & Navigation** section (Niche -> Kanban -> Episode -> Multi-channel workflow) and the **Industrial Brutalist / Marathon** design language. Unified all architectural, workflow, and data entity details into one design-oriented source of truth. |
| 2026-03-29 | **Settings page UX redesign complete**: replaced input+datalist model pickers with `<select>` dropdowns populated from MODEL_CATALOG per provider, removed all verbose copy/description text from pipeline stage cards, merged the redundant Codex+OpenAI health cards into a single "OpenAI API" status card, widened the card grid from 200px to 260px min-width, reduced oversized metric-value font size, and cleaned up verbose notices across both the Settings page and the niche-project provider config section. Also completed the full Codex CLI removal: routed both `codex` and `openai` providers through the OpenAI HTTP API, removed all Codex CLI infrastructure (`codex_bin`, `_probe_codex`, env vars, agent config files), added deep nested error extraction for readable API error messages, added collapsible error details in episode UI, fixed OpenAI strict-mode schema validation, and added stage-run cleanup on episode resume to prevent stale error log confusion. All 8 CLI runner tests passing. |
| 2026-03-29 | **TTS throughput stabilization complete**: repaired the dashboard Python environment to use `torch 2.3.1+cu121` / `torchaudio 2.3.1+cu121`, so XTTS now reports `device = cuda` on the RTX 3050 Laptop GPU instead of falling back to `torch 2.3.1+cpu`. Backend TTS health payloads now include runtime device/build metadata plus active/queued generate counts, paused-TTS recovery requeues orphaned `processing` jobs whose worker has no fresh heartbeat even when another worker is alive, duplicate worker starts are blocked when a fresh heartbeat already exists, production `generate` jobs enforce a minimum `chunk_max_chars` of `260` while `Play test` keeps the saved per-profile chunk size, and worker progress writes are throttled to reduce SQLite churn while preserving final chunk totals. The voice-profiles page and paused-TTS episode surfaces now show CPU-vs-GPU plus queue-depth telemetry so slow CPU execution no longer looks like a dead queue. Verified with `python -m compileall tool1_dashboard run_tool1_dashboard.py tests/test_tts.py`, `python -m pytest tests/test_tts.py -q` (`57` passing before and after the CUDA install), runtime inspection showing `torch 2.3.1+cu121`, `cuda_available = True`, and `gpu_name = NVIDIA GeForce RTX 3050 Laptop GPU`, plus a real XTTS worker smoke on a temporary DB where `test_voice` and a 7-chunk `generate` job both completed successfully on CUDA. |
| 2026-03-28 | **Drawbridge TTS queue-state clarity complete**: stopped marking queued narration jobs as `running` in the backend episode-language status table, so only the worker-claimed TTS job renders as `Running` while the remaining languages stay `Queued`. Updated the UI summaries, per-language badges, compact progress copy, and retry gating to reflect the serial XTTS worker more honestly. Added regression coverage for initial queueing, queued-vs-processing reconciliation, and single-language retry queueing. Verified with `python -m pytest tests/test_tts.py` (`49` passing). |
| 2026-03-28 | **Drawbridge live-activity signal clarity complete**: added preview-file metadata to serialized stage runs (`stdout_updated_at`, `stderr_updated_at`, `stdout_size_bytes`, `stderr_size_bytes`), rebuilt the episode `Live activity` panel into explicit `Run age` / `Output source` / `Last preview write` cards, and separated real stdout/stderr previews from the fallback execution snapshot so users can tell whether a provider stage is actively producing output or only exposing its command context. Verified with `node --check tool1_dashboard/ui/app.js`, the full `python -m unittest discover -s tests -v` suite (`143` passing), and Playwright smoke on `http://127.0.0.1:8765/#/niche-projects/niche-20260326-133703-religi-o`, opening the real episode overlay and confirming the new live-activity cards plus preview/file metadata in the DOM and viewport screenshot. |
| 2026-03-28 | **Drawbridge stale paused-TTS recovery complete**: routed episode TTS submission through the shared `Tool1Service.tts_manager`, added paused-episode recovery that requeues stale `processing` narration jobs and wakes the shared worker when multilingual TTS batches are stranded after a restart, and hardened paused-TTS reconciliation so worker-resume failures become explicit episode errors instead of an infinite `paused_for_tts` stall. Added regression coverage for a paused episode with a stale `processing` TTS job, verified with `python -m pytest tests/test_video_pipeline.py -k "paused_tts_episode or live_tts_progress"` (`5` passing). |
| 2026-03-28 | **Drawbridge modal sidebar removal and paused-TTS repair complete**: removed the episode overlay sidebar so quick actions and stage-run history now live only in the main column, updated `paused_for_tts` frontend copy to reflect real narration wait states, and rewired the backend TTS entry/reconciliation flow so all eligible languages receive jobs before the episode pauses. Paused-TTS recovery now refreshes running jobs, backfills legacy `pending` languages missing `tts_job_id`, resumes to `alignment` only when every language is terminal, and fails unrecoverable states with explicit error copy instead of leaving cards visually stuck in `running`. Added regression coverage for multi-language TTS queueing, paused-TTS completion handoff, and legacy backfill recovery. Verified with `python -m py_compile tool1_dashboard/service.py`, `node --check tool1_dashboard/ui/app.js`, targeted plus full `python -m pytest tests/test_video_pipeline.py` runs (`51` passing), and Playwright smoke on `http://127.0.0.1:8020/#/niche-projects/niche-20260326-133703-religi-o`, confirming the single-column overlay and real episode `204` showing live `TTS / Voice` progress (`1/5` done, `4` running) instead of the old single-job stuck state. |
| 2026-03-28 | **Codex Run button setup complete**: added a shared local-environment file at `.codex/environments/environment.toml` with a header `Run Tool 1 Dashboard` action that runs `python run_tool1_dashboard.py`, so this workspace can expose Codex's project Run button from repo state instead of requiring manual chat setup each time. Verified by TOML parsing the new file and importing `run_tool1_dashboard` with the same default `python` launcher used by the action. |
| 2026-03-28 | **Drawbridge output-files list scroll retention complete**: tracked output-files list scroll state per episode, restored it after both full `renderApp()` rerenders and local `syncEpisodeFilesSection()` refreshes, and added contained overscroll behavior so the parent modal cannot steal the wheel gesture while browsing long artifact lists. Verified with the full `python -m unittest discover -s tests -v` suite (`141` passing) plus a Playwright probe on `http://127.0.0.1:8031` confirming the list stayed at `scrollTop = 220` after both refresh paths. |
| 2026-03-28 | **Drawbridge output-files panel fix complete**: replaced the episode detail/overlay output-files section with a live state-driven panel that refreshes automatically, removed the redundant manual refresh button, preserved nested files without collapsing duplicate basenames, labeled empty files clearly, and added inline preview/download support for text, JSON, audio, and non-previewable binary artifacts. Added API regression coverage for recursive file listing, empty-state previews, binary fallback copy, and downloads. Verified with `python -m py_compile tool1_dashboard/app.py`, `node --check tool1_dashboard/ui/app.js`, the full `python -m unittest discover -s tests -v` suite (`138` passing), and browser smoke confirming `18 files`, `17 with data`, `1 empty`, plus the empty-file preview message inside the episode overlay. |
| 2026-03-28 | **Workflow control pass complete**: added backend/manual workflow controls so failed or paused episodes resume from the real stopped stage by default, explicit selected-step reruns can reset downstream outputs safely, start-stage guardrails block invalid mid-pipeline restarts when required artifacts are missing, and pause requests now land at safe stage boundaries including after async TTS completion. The episode detail page and overlay now expose a dedicated workflow-control panel with `Start/Resume workflow`, `Run selected step`, and `Pause after current step`. Verified with `python -m py_compile tool1_dashboard/service.py tool1_dashboard/app.py tool1_dashboard/database.py tool1_dashboard/config.py`, `node --check tool1_dashboard/ui/app.js`, and the full `python -m pytest tests/test_video_pipeline.py` suite (`47` passing). |
| 2026-03-28 | **TTS progress visibility pass complete**: added structured live TTS telemetry to episode serialization (`active_tts_job`, per-language `tts_job_current_chunk`, `tts_job_total_chunks`, `tts_job_percent`, `worker_active`), updated the Kanban card and both episode detail surfaces to render active narration chunk progress with a live meter instead of a static paused-looking state, and added regression coverage so board/detail payloads preserve those fields. Verified with `python -m py_compile tool1_dashboard/service.py`, `node --check tool1_dashboard/ui/app.js`, `python -m pytest tests/test_video_pipeline.py -k "live_tts_progress or chunks_text_and_snapshots_profile_tuning or niche_project_episodes_appear_in_board"`, and `python -m pytest tests/test_tts.py -k "PausedTtsEpisodeTests"`. |
| 2026-03-27 | **Workflow-stage OpenAI provider support complete**: added `openai` as a first-class structured stage provider, stored a shared workflow-stage OpenAI API key in Settings, added `/api/providers/openai/discover` to scan and cache current OpenAI text models, switched stage model controls from locked selects to editable suggestion-backed inputs so new CLI model ids can be typed manually, and wired scene/style/prompt stages to execute through the OpenAI Responses API with JSON schema output when `OpenAI API` is selected. Also added a DB `get_setting()` helper, generated matching `config/agents/*/openai.md` prompt files, and covered the new flow with stage-provider discovery, queue-readiness, saved-key handoff, and OpenAI runner tests. Verified with `node --check tool1_dashboard/ui/app.js`, `python -m unittest discover -s tests -p "test_cli_runner.py" -v` (`8` passing), `python -m unittest discover -s tests -p "test_video_pipeline.py" -v` (`40` passing), the full `python -m unittest discover -s tests -v` suite (`130` passing), and Playwright smoke on `http://127.0.0.1:8020/#/settings`, confirming the new `OpenAI workflow access` card plus editable `Model id` fields and provider-switch fallback from `haiku` to `gpt-5.4-mini`. |
| 2026-03-27 | **Drawbridge provider timeout guard complete**: added a 600-second structured CLI timeout to `CliRunner`, wrote timeout-aware provider errors, exposed a DB query for running stage-runs, and taught the background worker to auto-fail stale provider stages into `Needs Attention` with a retryable timeout message instead of leaving episodes stuck in `Consistency Guide` forever. Verified with `python -m unittest discover -s tests -p "test_video_pipeline.py" -v` (`37` passing), `python -m unittest discover -s tests -p "test_cli_runner.py" -v` (`7` passing), API smoke on `http://127.0.0.1:8020/api/episodes/ep-20260327-140936-204`, and Playwright smoke confirming episode `204` now renders under `Needs Attention` with `Consistency Guide timed out after 600 seconds...`. |
| 2026-03-27 | **Drawbridge modal close button repair complete**: removed `onclick="event.stopPropagation()"` from `.board-modal-shell` and `.episode-quick-actions` wrappers which were inadvertently breaking the global event delegation logic (`document.addEventListener("click", ...)`). The modal close button, along with episode quick actions, now properly bubble their events to the global handler again. Status updated to done in Drawbridge tasks. |
| 2026-03-27 | **Drawbridge overlay live-run visibility fix complete**: added a visible `Live activity` panel to episode overlays, opened active stage-run cards by default, and fell back to a terminal-style metadata snapshot when a running provider stage has no `stdout/stderr` preview yet. That lets the user verify the current stage, provider, model, workdir, artifact folder, and whether the CLI is still waiting on output instead of seeing an apparently empty modal. Verified with `node --check tool1_dashboard/ui/app.js` and Playwright smoke on `http://127.0.0.1:8020/#/niche-projects/niche-20260326-133703-religi-o`, opening the real running episode `204` and confirming `Live activity` plus the run snapshot box were visible in the overlay. |
| 2026-03-27 | **Drawbridge paused-TTS Kanban placement fix complete**: adjusted the frontend `episodeDisplayStage()` logic so `paused_for_tts` cards keep the real `current_stage` instead of being forced back to `queued_from_stage`, which moved the live `Kanban Probe 20260327` card from `Consistency Guide` into `TTS / Voice` and upgraded the status copy from a generic wait message to live per-language TTS progress (`Generating TTS 1/5 done.`). Verified with `node --check tool1_dashboard/ui/app.js`, `python -m unittest discover -s tests -p "test_video_pipeline.py" -v` (`36` passing), and Playwright smoke on `http://127.0.0.1:8020/#/niche-projects/niche-20260326-133703-religi-o`, where the real paused episode rendered under the `TTS / Voice` column. |
| 2026-03-27 | **Drawbridge modal scroll state preservation complete**: fixed an issue where the 5-second background refresh loop wiped out scroll positions inside the episode overlay by adding `modalMainScrollTop` and `modalSideScrollTop` tracking to the global `captureDashboardScroll()` and `restoreDashboardScroll()` functions, ensuring the user's reading position in the right-pane or main-pane remains stable during pipeline progress updates. Verified with `node --check tool1_dashboard/ui/app.js` and git commit. |
| 2026-03-27 | **Drawbridge real workflow feedback on the project Kanban complete**: added an always-visible inline workflow status row to episode cards, fixed board column placement to use the same computed display stage as the status copy so started cards leave `Draft` immediately, mapped per-language status summaries to the real backend keys (`translation_status`, `tts_status`, `srt_status`, `timeline_status`), and shortened active-board polling from 5s to 1s while episodes are `queued`, `running`, or `paused_for_tts`. Verified with `node --check tool1_dashboard/ui/app.js`, targeted pytest for queue/requeue behavior, and Playwright smoke on `http://127.0.0.1:8021/#/niche-projects/niche-20260326-133703-religi-o`, where a fresh draft card moved from `Draft` to `Consistency Guide` with inline running status within roughly 100ms of workflow start. |
| 2026-03-27 | **Drawbridge episode start UX cleanup complete**: renamed the queue/requeue surface into explicit workflow language, replaced board and overlay start buttons with compact icon-only actions plus hover explanations, made readiness panels state-aware, added optimistic inline overlay feedback, and reconciled that feedback against later refreshes so fast provider failures replace stale success copy with `Workflow failed in Consistency Guide.`. Verified with `node --check tool1_dashboard/ui/app.js`, `python -m unittest discover -s tests -v` (`124` passing), and Playwright smoke on seeded ready/blocked project boards plus a mobile-width overlay pass. The live restart smoke hit a real provider limitation (`Claude limit reached. You've hit your limit · resets Mar 28, 5pm (America/Sao_Paulo)`), which the UI now surfaces correctly inline. |
| 2026-03-27 | **Drawbridge sidebar utility relocation complete**: moved the global `Refresh data` and theme-toggle controls from the top-right topbar into the left sidebar as compact quick-action buttons, so the header now shows only the active page title plus notices while the shell actions stay in the lateral menu. Verified with Playwright smoke on `http://127.0.0.1:8021/#/translation-profiles`, confirming the topbar no longer contains those controls and the sidebar exposes both actions in collapsed mode. |
| 2026-03-27 | **Drawbridge translation-profile card simplification complete**: removed the repeated translation-profile section copy and long helper text from the page, rebuilt the default cards into compact summary buttons that show only name/provider/model/readiness, and reused the existing translation-profile editor modal as the details surface on click while keeping edit/delete actions separate. Verified with `node --check tool1_dashboard/ui/app.js` and Playwright smoke on `http://127.0.0.1:8020/#/translation-profiles`, including summary-click modal open, delete-dialog isolation, and mobile-width snapshot checks. |
| 2026-03-27 | **Drawbridge project-config disclosure stabilization complete**: replaced the niche-project config panels' native `<details>` behavior with explicit button-driven disclosure state in the frontend, kept `Language setup` and `Provider setup` stable across rerenders, and paused the 5-second auto-refresh loop while project-config controls are focused so dropdown work is not interrupted mid-edit. Verified with `node --check tool1_dashboard/ui/app.js`, `python -m unittest discover -s tests -v` (`124` passing), and Playwright smoke on `http://127.0.0.1:8020/#/niche-projects/niche-20260326-133703-religi-o`, confirming both panels stay open across >10 seconds of background refresh, remain stable while selects are focused, and toggle correctly via keyboard `Space` / `Enter` with `aria-expanded` updates. |
| 2026-03-26 | **Drawbridge translation-profile setup rework complete**: split Translation Profiles off the shared stage-provider catalog, added OpenAI model discovery plus sanitized API payloads, rebuilt the modal into a provider-mode create/edit flow with searchable/sortable discovered models and saved-key masking, and exposed preview-only `Codex CLI` / `Claude Code CLI` tabs with disabled save. Verified with `node --check tool1_dashboard/ui/app.js`, `python -m unittest discover -s tests -v` (`124` passing), and browser smoke on `http://127.0.0.1:8032/#/translation-profiles` covering create, placeholder tabs, edit rediscovery, model filtering, and saving an updated model via a mocked discovery route. |
| 2026-03-26 | **Drawbridge voice-card control cleanup complete**: compacted the voice-profile action row so play, tuning, and delete are all icon-only tooltip actions, tightened title/action alignment for long names, and kept `Starting voice engine` / `Generating sample` states compact with a small status pill plus short copy instead of bulky text buttons. Verified with `node --check tool1_dashboard/ui/app.js`, `python -m unittest discover -s tests -v` (`115` passing), and browser smoke on `http://127.0.0.1:8765/#/voice-profiles` confirming the new idle and generating states. |
| 2026-03-26 | **Per-voice TTS pacing control shipped**: added saved per-profile `tts_config` presets plus advanced tuning, exposed a compact `Tuning` modal on voice-profile cards, made the repo TTS chunker the single authoritative split layer for both `Play test` and production narration, disabled XTTS internal text splitting, and snapshotted the resolved tuning into queued TTS jobs so live jobs are stable across later profile edits. Verified with `node --check tool1_dashboard/ui/app.js`, `python -m unittest discover -s tests -v` (`115` passing), and browser smoke on `http://127.0.0.1:8765/#/voice-profiles` covering preset switching, advanced-value rewriting, and `Save and play test` driving the inline `Generating sample` state. |
| 2026-03-26 | **Automatic voice-engine lifecycle shipped**: removed first-party manual worker controls from Voice Profiles and episode TTS views, stopped auto-launching XTTS at app boot, added on-demand auto-start plus stale-worker recovery, and introduced smart idle shutdown with short interactive cooldowns and longer pipeline drain cooldowns. Voice Profiles now show `Starting voice engine` inline only while a user action wakes the engine, while episode views only surface true startup/runtime failures. Verified with `node --check tool1_dashboard/ui/app.js` and `python -m unittest discover -s tests -v` (`108` passing). |
| 2026-03-26 | **Voice sample playback stabilized**: paused the Voice Profiles auto-refresh loop whenever an inline preview clip is actively playing, preventing the 5-second dashboard rerender from replacing the audio element mid-playback. Verified with `node --check tool1_dashboard/ui/app.js` and `python -m unittest discover -s tests -v` (`101` passing). |
| 2026-03-26 | **Voice profile flow simplified**: removed language from the create-profile UI, stopped filtering/validating voice profiles by language in project assignment, moved default sample text generation to the backend, and rebuilt the voice-profile card around a one-click `Play test` action with inline fresh-sample playback. Verified with `node --check tool1_dashboard/ui/app.js`, `python -m unittest discover -s tests -v` (`101` passing), and browser smoke confirming create modal simplification, fresh-test generation state, and project language dropdowns showing the same voice profile for every language. |
| 2026-03-26 | **TTS Runtime Fixed**: Resolved multiple environment issues preventing `TTS.api` imports and worker startup. Pinned `torch` to `2.3.1`, `transformers` to `4.39.3`, and manually installed missing binary dependencies (`bangla`, `gruut`, `spacy[ja]`, `umap-learn`). Verified worker status as `idle` and healthy on port 8020. |
| 2026-03-26 | **Drawbridge voice-profile UX pass complete**: enriched voice-profile payloads with the latest latent-precompute and voice-test job metadata, moved voice testing into an inline card form, added per-card clone/test status messaging plus preview playback/download, and replaced the raw worker `stale` label with clearer restart guidance. Verified with `node --check tool1_dashboard/ui/app.js`, `python -m unittest discover -s tests -v` (`99` passing), and browser smoke on `http://127.0.0.1:8765/#/voice-profiles` confirming the inline test form, the new worker/card copy, and only the existing `favicon.ico` 404 in the console. |
| 2026-03-26 | **Drawbridge hover affordance polish complete**: collapsed the Draft-column add CTA into an icon-only `+` button with a hover tooltip, and moved column guidance copy into hover tooltips on the column titles so the kanban headers stay compact. Verified with `node --check tool1_dashboard/ui/app.js` plus browser smoke on `http://127.0.0.1:8765/#/niche-projects/niche-20260325-215207-audit-project`, including screenshots for the Draft `+` tooltip and the Draft-column title tooltip. |
| 2026-03-26 | **Drawbridge UI feedback pass complete**: renamed the project-board episode CTA and the create-episode modal copy from `Create draft` to `Create episode` while keeping the Draft-only helper text, and replaced the create-project target-language checkbox wall with a searchable picker that adds selected languages into removable pills below the input. Verified with `node --check tool1_dashboard/ui/app.js`, `python -m unittest discover -s tests -v` (`98` passing), and browser smoke on `http://127.0.0.1:8031` covering the project-board CTA plus the new create-project language picker flow. |
| 2026-03-26 | **Interaction latency pass complete**: route changes and episode overlay opens now paint an immediate loading state before background data hydration finishes, refreshes no longer pull every heavyweight endpoint on every interaction, and provider health probes are cached briefly inside `CliRunner` to avoid repeated CLI subprocess checks. Verified with `node --check tool1_dashboard/ui/app.js`, the full `python -m unittest discover -s tests -v` suite (`98` passing), and browser smoke showing the episode overlay loading shell appears instantly on click before the full detail payload arrives. |
| 2026-03-26 | Added TTS runtime preflight and fail-fast UX: the worker health endpoint now reports missing XTTS dependencies, voice profile creation no longer queues dead latent jobs when the runtime is missing, and voice tests return an actionable error instead of sitting in `queued` forever. Verified with targeted TTS tests and an API smoke call returning `503` plus the startup error payload. |
| 2026-03-26 | **Project-Scoped Workflow Repair complete**: the project page is now the primary Kanban surface, new episodes stay in Draft until explicitly queued, episode details open as an overlay, queue/requeue is blocked by structured readiness checks, provider-stage failures preserve full actionable logs, and template/settings reads no longer mutate template state. Verified with 93 passing tests, `node --check tool1_dashboard/ui/app.js`, and browser smoke covering project board, draft creation, overlay routing, and blocked queue UI. |
| 2026-03-26 | **Frontend UI Overhaul Complete**: Executed an 8-phase densification and cleanup of the dashboard. Replaced heavy \`.detail-section\` wrappers with clean \`.surface\` grids. Added collapsible icon sidebar. Widen Kanban columns. Stripped redundant "eyebrows" and helper text. Added tactile micro-animations to cards and buttons. Merged Settings and Niche Project configuration cards into tighter grids. |
| 2026-03-25 | Phase 10 complete: Final Cleanup & Documentation — verified agent configs and dependencies, removed all remaining references to legacy architecture, all tests pass. Tool 1 is fully transitioned to the episode-first pipeline. |
| 2026-03-25 | Phase 9 complete: Review & Export Phase — timeline editor, consistency guide editor, prompt list editor, per-language timeline read-only view, and fully wired UI handlers for saving review data and finalizing/downloading export zip. |
| 2026-03-25 | Phase 8 complete: TTS & translation polish — per-language retry (translation/TTS), translation preview (side-by-side), TTS worker health indicator, TTS job progress in episode detail, retry buttons on failed languages. 87 tests passing. |
| 2026-03-25 | Phase 7 complete: niche project detail enhancement — stats bar, inline language config with voice/translation profile dropdowns, AI provider/model config, enhanced episode cards with per-language mini-dots, batch operations (queue drafts, re-run failed), added missing `/api/target-languages` endpoint. 87 tests passing. |
| 2026-03-25 | Phase 6 complete: enhanced Episode Pipeline Board with elapsed timers, progress bars, quick actions (Queue, Delete), output file previews, and expandable stage run details with stdout/error logs. Added `/api/episodes/{id}/files` endpoint. |
| 2026-03-25 | Phase 5 complete: removed ~2100 lines of legacy frontend code from app.js (3854→1717 lines). Removed legacy sidebar items, routing, render functions, state, event handlers. Default route changed to pipeline-board. 87 tests pass, 0 JS errors |
| 2026-03-25 | Phases 2-4 complete: removed all legacy code (jobs/projects/builds). database.py rewritten with niche_projects table, service.py reduced from 4500→1737 lines, app.py from 849→496 lines, 87 tests passing |
| 2026-03-25 | Phase 1 complete: deleted 4 standalone tools, archived outdated docs, committed all uncommitted code, 119 tests passing |
| 2026-03-25 | Reconstructed 10-phase implementation plan (saved to IMPLEMENTATION_PLAN.md) |
| 2026-03-25 | Created PROJECT_REGISTRY.md for cross-conversation continuity |
| Pre-2026-03-25 | Built multilingual episode pipeline, translation module, TTS module, UI |
